/// PRO PHASE: IGNITION PROTOCOL [V28 MASTER]

1. PHASE 1: KERNEL SINGLETON ASSIGNMENT
Action: Immediate assignment of `window.RIYAS_SYSTEM = CoreLogics`.
Purpose: [ID 3385] Prevents reference race conditions. Ensures the CoreLoop and CursorService can resolve the system brain during the very first execution frame.
2. PHASE 2: DOM MOUNTING & CONTEXT PREPARATION
Action: Execution deferred until `DOMContentLoaded`.
Purpose: [ID 3380] Ensures the `#webgl-canvas` and `#os-greeting` layers are present in the DOM before the Renderer attempts context acquisition, preventing null-pointer crashes.
3. PHASE 3: THE HARDWARE HANDSHAKE (LOGIC ENGINE)
Action: CoreLogics initializes background asset preloading.
Critical Logic: The logic engine must finish asset registration and scene setup before visual effects are attached to ensure effect groups aren't purged by scene overrides.
4. PHASE 4: AUDIO & HEARTBEAT IGNITION
Action: `Audio.init()` and `CoreLoop.start()` fire.
Purpose: Registers the AudioContext to await user interaction and starts the central heartbeat to drive physics and manifesto animations.
5. PHASE 5: MANIFESTO IGNITION (8bit.ai VISUALS)
Action: `CoreManifesto.init(CoreScene.get())` fires immediately during boot.
Purpose: [ID 4341] Ensures kinetic background streaks are visible behind the boot terminal immediately, establishing the high-contrast 8bit.ai aesthetic.
6. PHASE 6: UI BRIDGE RESTORATION (GREETING)
Action: Instantiation of `new Greeting()`.
Purpose: Starts the typewriter insight loop and reveals the "Enter System" prompt.
7. PHASE 7: THE LENS THUMP (SYSTEM UNLOCK)
Action: User clicks "Enter System" -> `unlockSystem()`.
Critical Teardown:

* `CoreManifesto.dispose()` purges background WebGL memory.
* `window.RIYAS_SYSTEM.init()` reveals the pre-constructed universe.
* Lens-thump CSS triggers and the camera lerps to the first planetary sector.

8. PHASE 8: NEURAL BRIDGE WIRING
Action: `GeminiInterface.init()` and `TerminalBridge.init()` fire.
Purpose: Bootstraps the Cognitive UI Shard and hijacks the terminal CLI to redirect natural language queries to the Dify.ai gateway.
9. PHASE 9: MASTER SYNCHRONIZATION (RIYAS_OS_READY)
Action: `main.js` dispatches `CustomEvent('RIYAS_OS_READY')`.
Purpose: Master sync signal telling secondary systems (VFX, Cursor) that the neural bridge is active and the scene is ready for effect mounting.
10. PHASE 10: HEARTBEAT GATING
Action: `Loop.js` monitors `CoreManifesto.isActive`.
Purpose: [ID 4386] Ensures background calculations for 8bit.ai visuals are zeroed out after OS unlock, reclaiming 100% of GPU resources for planetary rendering.